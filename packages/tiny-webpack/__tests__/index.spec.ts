import { parseFile, generateGraph, printCode } from '../src';

const entry = './data/a.js';

test('parseFile', () => {
  expect(parseFile(entry)).toEqual(null);
});

test('generateGraph', () => {});

test('printCode', () => {});
